# 구현 상태와 검증 기록

이 문서는 개발 중 관찰한 결과를 기록합니다. 새 설치는 [README](README.md), 서버 설치는 [배포 안내](continuity-bridge/deploy/lxc/README.md), 업데이트는 [업데이트 안내](continuity-bridge/UPGRADE.md)를 따릅니다. 개인 운영 서버의 주소·토큰·기기 식별자는 설치 예제로 사용하지 않습니다.

## 현재 구현

- Android에 로컬 ADB 클립보드 도우미를 내장했습니다. 별도 Shizuku 관리자 없이 최초 무선 페어링을 진행합니다.
- 텍스트·PNG/JPEG 전송, 원격 적용 확인과 ACK, 대기 이벤트 저장을 구현했습니다.
- Android에서 알림을 보낼 앱을 미리 선택합니다. 시스템 상태·충전 알림을 걸러내고 작업 진행률을 전달합니다.
- Mac은 원래 앱 아이콘이 있는 전용 배너, 최근 100개 알림, 진행률 갱신, 배너 일시 중지를 제공합니다.
- 릴레이 역할 토큰과 선택 가능한 기기별 Cloudflare Access 서비스 토큰을 사용합니다. HTTP 리디렉션을 따르지 않습니다.

## 과거 실행 검증

아래는 해당 날짜의 관찰 기록입니다. 현재 실행 상태나 모든 장치의 호환성을 보장하지 않습니다.

| 날짜 | 환경 | 확인한 범위 |
|---|---|---|
| 2026-09-07 | Mac · API 37 AVD · NPM · Debian LXC | Access 인증, 양방향 텍스트, 알림, publish/fetch/ACK와 long-poll |
| 2026-09-08 | 격리 Mac 테스트 앱 · API 37 AVD · 로컬 릴레이 | 사진 크기·형식 경계, 원본 바이트, 알림 아이콘·진행률·필터 |
| 2026-09-13 | API 37 arm64 AVD, 16 KiB 페이지 | 앱 내부 페어링, shell UID 2000 도우미, 키보드 유지, 재부팅·도우미 종료 복구 |
| 2026-09-14 | Galaxy SM-S948N · Android 16/API 36 · Mac | 내장 도우미 설치·최초 페어링, 텍스트 10회, Galaxy → Mac PNG/JPEG |

### 2026-09-14 Galaxy 결과

설정과 암호화된 자격증명을 유지한 채 APK를 업데이트했습니다. Samsung 설정과 브리지의 분할 화면에서 시스템 코드를 입력해 페어링했습니다.

자동 검색이 과거 무선 디버깅 포트를 선택하는 문제를 디버거로 확인했습니다. 같은 폰의 검색 결과에서 닫힌 포트를 건너뛰고, 루프백 연결이 가능한 포트를 선택하도록 수정했습니다. 회귀 테스트는 수정 전 2개 실패에서 수정 후 5개 모두 통과했습니다.

- 텍스트 10/10회가 실제 Mac 앱에 전달됐습니다.
- 편집기 포커스 이탈과 삼성 키보드 숨김은 각각 0회였습니다. 복사 호출 자체는 2–6ms였습니다.
- ADB 터치부터 Mac 검사기 확인까지 374–719ms, 중앙값 499ms였습니다. 도구 실행 시간이 포함되므로 순수 네트워크 지연이나 성능 보장값이 아닙니다.
- Galaxy → Mac PNG 3,701바이트와 JPEG 3,999바이트는 원본 해시와 일치했습니다.
- Mac → Galaxy JPEG 붙여넣기는 원본과 일치했지만 재전송 방지 검사는 통과하지 못했습니다. 원인과 양방향 사진 회귀 검증이 남아 있습니다.

이 실행에서 확인한 APK SHA-256은 `40042909e8cff6c292f54c02161fa43597d63f97e038d0e0798bdaacba4a86c4`입니다. 배포 파일을 새로 빌드하면 체크섬은 바뀔 수 있습니다.

## 2026-09-14 공유 준비 검증

- 개인 runtime·기존 서명 키·빌드 캐시가 없는 별도 복사본에서 Android와 Mac 패키징을 확인했습니다. Mac 기본 산출물에는 개인 CA나 고정 서버 핀이 포함되지 않습니다.
- 프로토콜 계약 146개 항목, 릴레이 81개, Mac 125개, 통합 검증기 49개 검사가 통과했습니다. Android 호스트·이미지·알림·Access 검사, 도우미 discovery 5개와 lifecycle 12개도 통과했습니다.
- TLS 생성·재사용·거부·복구 11개 검사 그룹이 통과했습니다. Android 자체 CA 검사는 명시한 CA와 APK의 일치, 다른 CA 거부, 암묵적 교체 거부를 확인합니다.
- 새 인증서·토큰으로 임시 릴레이를 실행하고 HTTPS readiness, 인증 누락 거부, 전송 후 프로세스 재시작·수신·ACK·큐 제거를 확인했습니다.
- 최초 등록용 ID 추출 명령은 합성 JSON/XML로 확인했습니다. 이번 공유 준비 작업에서는 휴대폰, 설치된 Mac 앱, 실제 LXC/NPM을 다시 조작하지 않았습니다.

## 아직 확인하지 않은 범위

- 위 Galaxy 빌드에서 재부팅·잠금·도우미 강제 종료 후 자동 복구. 당시 자동 복구 옵션은 꺼져 있었습니다.
- Android 11/12 최초 연결, x86_64 네이티브 실행, 업무 프로필 및 제조사 전체 호환성.
- 모든 Android 앱의 사진 URI 제공 방식과 잠금 상태 알림.
- Mac 집중 모드와 전용 배너의 연동. 현재 별도 동작입니다.

AVD 결과를 실기기 결과로 바꿔 표현하지 않습니다. 자동 검사 명령은 [빌드 안내](continuity-bridge/BUILD.md)에 있습니다. 비공개 인증 정보, 기기 화면과 개인 작업 경로는 저장소에 포함하지 않습니다.
